{% extends 'admin_base.html.twig' %}

{% block title %}ImagesVoitures{% endblock %}

{% block body %}
    <h1>ImagesVoitures</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ images_voiture.id }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ images_voiture.image }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_images_voitures_index') }}">back to list</a>

    <a href="{{ path('app_admin_images_voitures_edit', {'id': images_voiture.id}) }}">edit</a>

    {{ include('admin_images_voitures/_delete_form.html.twig') }}
{% endblock %}
